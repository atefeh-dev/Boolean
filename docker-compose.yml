services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: sidebar
      POSTGRES_PASSWORD: sidebar
      POSTGRES_DB: sidebar
    ports:
      - "5432:5432"
    volumes:
      - sidebar_pgdata:/var/lib/postgresql/data

volumes:
  sidebar_pgdata:
